arch:
  children:
    personal:
    work:

personal:
  hosts:
    arch-dev-machine:
      git_name: Caio Delgado         # same as git config --global user.name
      git_email: caio@nxt.sh   # same as git config --global user.email
      virtual_machine: true       # install and secure qemu-guest-agent    [true/false]
      desktop_environment: true   # install desktop environment            [true/false]
      ghostty_install: false       # install ghostty (beta access)          [true/false]

    synology-vm:
      virtual_machine: true
      desktop_environment: true

    lenovo-arch:
      desktop_environment: true

work:
  hosts:
    workbox:
      virtual_machine: true
